display_location:
  type: enum
  default: "Both"
  choices:
    - Top
    - Bottom
    - Both
  description: "Where should the topic timer appear?"

use_parent_for_link:
  type: bool
  default: false
  description: "Use parent name for category link in topic timer banner?"

enabled_category_slugs:
  type: list
  default: ""
  description: "Only apply this component to topics in these category slugs (one per line)"
